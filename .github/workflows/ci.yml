name: ci

on:
    push:
        branches:
            - main
    pull_request:

permissions:
    contents: read

concurrency:
    group: ci-${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    lint:
        runs-on: ubuntu-latest
        timeout-minutes: 10
        steps:
            - uses: actions/checkout@v4
            - uses: JohnnyMorganz/stylua-action@v4
              with:
                  version: latest
                  token: ${{ secrets.GITHUB_TOKEN }}
                  args: --check .

    test:
        runs-on: ubuntu-latest
        timeout-minutes: 20
        env:
            TABULATURE_NVIM_PATH: ${{ github.workspace }}/.dependencies/tabulature.nvim
        steps:
            - uses: actions/checkout@v4
            - uses: actions/checkout@v4
              with:
                  repository: griwes/tabulature.nvim
                  ref: main
                  path: .dependencies/tabulature.nvim
            - uses: rhysd/action-setup-vim@v1
              with:
                  neovim: true
                  version: stable
            - name: Run tests
              run: scripts/ci/test.sh

    tabulature-compatibility:
        runs-on: ubuntu-latest
        timeout-minutes: 20
        env:
            STATUESQUE_NVIM_PATH: ${{ github.workspace }}
        steps:
            - uses: actions/checkout@v4
            - uses: actions/checkout@v4
              with:
                  repository: griwes/tabulature.nvim
                  ref: main
                  path: .dependencies/tabulature.nvim
            - uses: rhysd/action-setup-vim@v1
              with:
                  neovim: true
                  version: stable
            - name: Run Tabulature against this Statuesque checkout
              working-directory: .dependencies/tabulature.nvim
              run: scripts/ci/test.sh
